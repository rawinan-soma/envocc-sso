# ─── Secrets & credentials ───────────────────────────────────────────────────
.env
.env.*
!.env.example
*.env
secrets/
*.p12
*.jks
*.keystore
*.pem
*.key
*.crt
*.pfx

# Full (un-stripped) realm exports — only stripped export is committed
*.full-export.json

# ─── Keycloak / Docker ───────────────────────────────────────────────────────
keycloak/data/
keycloak/themes/custom/
# Docker override files with secrets
docker-compose.override.yml
docker-compose.local.yml
.docker/

# ─── Node.js ─────────────────────────────────────────────────────────────────
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnp/
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
dist/
build/
.next/
.nuxt/
.output/
.cache/
.parcel-cache/
*.local

# ─── OS / Editor ─────────────────────────────────────────────────────────────
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
ehthumbs.db
.idea/
.vscode/
*.swp
*.swo
*~

# ─── Test libraries (vendored via git clone, not committed) ──────────────────
tests/lib/

# ─── Claude Code internal ─────────────────────────────────────────────────────
# (keep .claude/ tracked — it holds project settings, not secrets)
# .worktrees is ephemeral scratch space
.worktrees/
